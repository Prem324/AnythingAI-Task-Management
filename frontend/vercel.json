{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "env": {
    "VITE_API_URL": "@VITE_API_URL"
  },
  "routes": [
    {
      "src": "/[^.]+$",
      "dest": "/index.html"
    }
  ]
}
